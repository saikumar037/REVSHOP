# 📊 1. ERD (Entity Relationship Diagram)

## 🏗️ Database Schema Visualization:

# Entity Relationship Diagram

```text
┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐
│      USERS      │      │     SELLERS     │      │    PRODUCTS     │
├─────────────────┤      ├─────────────────┤      ├─────────────────┤
│ user_id (PK)    │◄─────┤ seller_id (PK)  │─────►│ seller_id (FK)  │
│ email           │      │ business_name   │      │ product_id (PK) │
│ password        │      │ business_address│      │ name            │
│ first_name      │      │ tax_id          │      │ description     │
│ last_name       │      │ business_phone  │      │ category        │
│ phone           │      └─────────────────┘      │ price           │
│ address         │                               │ mrp             │
│ user_type       │      ┌─────────────────┐      │ discount_price  │
│ created_at      │      │      CART       │      │ stock_quantity  │
│ is_active       │      ├─────────────────┤      │ threshold_qty   │
│ security_q      │      │ cart_id (PK)    │◄─────┤ product_id (FK) │
│ security_a      │      │ buyer_id (FK)   │      │ created_at      │
│ password_hint   │      │ product_id (FK) │      │ is_active       │
└─────────────────┘      │ quantity        │      └─────────────────┘
        │                │ added_at        │                │
        │                └─────────────────┘                │
        │                        │                          │
        │                        │                          │
        ▼                        ▼                          ▼
┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐
│     ORDERS      │      │   ORDER_ITEMS   │      │     REVIEWS     │
├─────────────────┤      ├─────────────────┤      ├─────────────────┤
│ order_id (PK)   │◄─────┤ order_id (FK)   │─────►│ order_id (FK)   │
│ buyer_id (FK)   │      │order_item_id(PK)│      │ review_id (PK)  │
│ order_date      │      │ product_id (FK) │◄─────┤ product_id (FK) │
│ total_amount    │      │ quantity        │      │ buyer_id (FK)   │
│ shipping_addr   │      │ price           │      │ rating          │
│ billing_addr    │      └─────────────────┘      │ comment         │
│ status          │                               │ review_date     │
│ payment_method  │      ┌─────────────────┐      └─────────────────┘
│ payment_status  │      │  NOTIFICATIONS  │
└─────────────────┘      ├─────────────────┤      ┌─────────────────┐
        │                │ notification_id │      │    FAVORITES    │
        │                │ user_id (FK)    │      ├─────────────────┤
        └───────────────►│ message         │      │ favorite_id (PK)│
                         │ type            │      │ buyer_id (FK)   │
                         │ is_read         │      │ product_id (FK) │
                         │ created_at      │      │ added_at        │
                         └─────────────────┘      └─────────────────┘
```

## 🔑 Relationships Summary

```text
┌───────────────────────────┬────────┬──────────────────────────────────────────────────┐
│       RELATIONSHIP        │  TYPE  │                   DESCRIPTION                    │
├───────────────────────────┼────────┼──────────────────────────────────────────────────┤
│ Users → Sellers           │  1:1   │ A user can register as a seller.                 │
│ Users → Buyers            │  1:1   │ A user can act as a buyer.                       │
│ Sellers → Products        │  1:N   │ A single seller can list multiple products.      │
│ Buyers → Cart             │  1:N   │ A buyer can have multiple items in their cart.   │
│ Products → Cart           │  1:N   │ A product can be added to many users' carts.     │
│ Buyers → Orders           │  1:N   │ A buyer can place multiple orders over time.     │
│ Orders → Order Items      │  1:N   │ An order contains multiple product line items.   │
│ Products → Order Items    │  1:N   │ A product can appear in many different orders.   │
│ Orders → Reviews          │  1:N   │ An order can receive multiple reviews.           │
└───────────────────────────┴────────┴──────────────────────────────────────────────────┘
```